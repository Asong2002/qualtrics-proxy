{
  "name": "arin-chat-bot",
  "version": "1.0.0",
  "description": "Arin AI Chat Bot deployed on Vercel",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step needed'",
    "start": "vercel dev",
    "deploy": "vercel deploy --prod"  // 新增部署命令，一键部署到生产环境
  },
  "dependencies": {
    // 补充Edge Functions可能需要的基础依赖（根据实际功能添加）
    "itty-router": "^4.0.23"  // 轻量路由库，适合Edge环境处理请求
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "keywords": ["chatbot", "ai", "vercel", "edge-function", "serverless"],
  "author": "Your Name",
  "license": "MIT"
}
